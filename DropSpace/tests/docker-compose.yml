version: '3.8'

services:
  web:
    build:
      context: ../
      dockerfile: ../Dockerfile
    image: $DOCKER_HUB_USERNAME/dropspacenginx:latest
    command: bash -c "sleep 10 && dotnet test"


#Потом можно будет добавить postgres, redis 
version: '3.8'

services:
  dropcontainer:
    build:
      context: ../
      dockerfile: ../Dockerfile
    image: dropcontainer:test
    command: bash -c "dotnet test"



#Потом можно будет добавить postgres, redis 